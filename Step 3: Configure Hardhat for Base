Update hardhat.config.js to include the Base network:javascript

require("@nomicfoundation/hardhat-toolbox");
require("dotenv").config();

module.exports = {
  solidity: "0.8.20",
  networks: {
    base: {
      url: "https://mainnet.base.org", // Base mainnet RPC
      accounts: [process.env.PRIVATE_KEY], // Your wallet’s private key
    },
    baseSepolia: {
      url: "https://sepolia.base.org", // Base testnet RPC
      accounts: [process.env.PRIVATE_KEY],
    },
  },
};

Create a .env file in your project root and add your wallet’s private key:

PRIVATE_KEY=your_private_key_here

Note: Never share your private key or commit it to version control.

